import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "Book Management Application" />
      <CardContent> A platform to manage books, authors, and genres < /CardContent>
      </Card>
    );