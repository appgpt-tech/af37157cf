import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "CardCollector" />
      <CardContent> A application for managing card collections < /CardContent>
      </Card>
    );