// Source code generated by AppGPT (www.appgpt.tech)

//Class to create tables and seed new database
import { DataSource } from "typeorm";
import { DBConfiguration } from "./Configuration";
import { SettingsEntity } from "./db/Settings.entity";
//autogenerate imports based on resources
import { UsersEntity } from "./db/Users.entity";
import { CardsEntity } from "./db/Cards.entity";
import { SetsEntity } from "./db/Sets.entity";
import { InventoryEntity } from "./db/Inventory.entity";
import { WishlistEntity } from "./db/Wishlist.entity";

export class Database {
  static dbConfiguration: DBConfiguration;
  public static ds: DataSource;

  static async Initialize(dbConfiguration: DBConfiguration) {
    Database.dbConfiguration = dbConfiguration;
    let dbConfig: any = dbConfiguration as any;
    //Autogenerate entities array from resource names

    dbConfig.entities = [
      SettingsEntity,
      UsersEntity,
      CardsEntity,
      SetsEntity,
      InventoryEntity,
      WishlistEntity,
    ];
    Database.ds = new DataSource(dbConfig);
    await Database.ds.initialize();

    //TODO: Drop all tables

    await Database.Seed();
  }
  static async Seed() {
    let data: any = {
      Users: [
        { userName: "Ash Ketchum", email: "ash@pokevault.com" },
        { userName: "Brock", email: "brock@pokevault.com" },
        { userName: "Misty", email: "misty@pokevault.com" },
      ],
      Cards: [
        {
          cardName: "Pikachu",
          serial: "PK001",
          type: "Electric",
          rarity: "Common",
          condition: "Good",
          imageUrl: "pikachu.png",
          set: "Base Set",
        },
        {
          cardName: "Charizard",
          serial: "CH001",
          type: "Fire",
          rarity: "Rare",
          condition: "Excellent",
          imageUrl: "charizard.png",
          set: "Base Set",
        },
        {
          cardName: "Bulbasaur",
          serial: "BU001",
          type: "Grass",
          rarity: "Common",
          condition: "Good",
          imageUrl: "bulbasaur.png",
          set: "Base Set",
        },
      ],
      Sets: [
        { setName: "Base Set", releaseDate: "1999-01-09", totalCards: 102 },
        { setName: "Jungle", releaseDate: "1999-06-16", totalCards: 64 },
        { setName: "Fossil", releaseDate: "1999-10-10", totalCards: 62 },
      ],
      Inventory: [
        { user: "Ash Ketchum", card: "Pikachu", dateAdded: "2022-01-01" },
        { user: "Brock", card: "Charizard", dateAdded: "2022-01-02" },
        { user: "Misty", card: "Bulbasaur", dateAdded: "2022-01-03" },
      ],
      Wishlist: [
        { user: "Ash Ketchum", card: "Charizard", dateInserted: "2022-02-01" },
        { user: "Brock", card: "Bulbasaur", dateInserted: "2022-02-02" },
        { user: "Misty", card: "Pikachu", dateInserted: "2022-02-03" },
      ],
    };
    //Autogenerate multiple such calls ie for each resource and its data object
    let isSeeded = await this.IsSeeded();
    //if (!isSeeded) {
    //forcing app recreation
    if (true) {
      console.log("   Seeding database...");
      await this.SeedResource("UsersEntity", data.Users);
      await this.SeedResource("CardsEntity", data.Cards);
      await this.SeedResource("SetsEntity", data.Sets);
      await this.SeedResource("InventoryEntity", data.Inventory);
      await this.SeedResource("WishlistEntity", data.Wishlist);
      await this.SeedResource("SettingsEntity", {
        settingname: "isSeeded",
        settingvalue: "true",
      });
    } else {
      console.log("   Database seeded already!");
    }
  }
  static async IsSeeded() {
    const repo = Database.ds.getRepository("SettingsEntity");
    let rec: any = await repo.findOne({
      select: {
        settingname: true,
        settingvalue: true,
      },
      where: {
        settingname: "isSeeded",
      },
    });
    if (rec && rec.settingvalue) return true;
    return false;
  }
  static async SeedResource(resourceName: any, resourceData: any) {
    const repo = Database.ds.getRepository(resourceName);
    //await repo.clear();
    console.log("   Seeding table " + resourceName);
    await repo.upsert(resourceData, ["id"]);
  }
}
